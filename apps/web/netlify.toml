[build]
  command = "cd ../.. && pnpm install && pnpm --filter @workspace/web run build"
  publish = "dist/public"

[functions]
  directory = "../../netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
