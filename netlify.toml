[build]
  command = "pnpm install && pnpm --filter @workspace/web run build"
  publish = "apps/web/dist/public"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
